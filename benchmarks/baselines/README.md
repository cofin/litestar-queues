# Queue benchmark baselines

Promote paired `.json` and `.md` artifacts here only from a clean release
checkout. Name them `YYYY-MM-DD-<environment>` and never replace an existing
dated result; dependency, image, hardware, or configuration changes create a
new baseline.

Benchmark runs are manual and local. Nothing in CI generates or updates this
directory automatically.
